# cdwindow

This program uses dbus-monitor output to save the most recently opened directory in nautilus
running the cdwindow command than changes to the saved directory in your choice of terminal
this is useful in Gnome, where only Gnome Terminal is supported by right click -> open in terminal

## Install 
```bash
example command
```
example

